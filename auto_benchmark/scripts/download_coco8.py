"""
Download the COCO8 mini dataset from Ultralytics for testing YOLO benchmarks.

COCO8 is a tiny 8-image dataset (4 train + 4 val) that ships with Ultralytics
and is useful for verifying that the training pipeline runs end-to-end.

Usage:
    uv run python scripts/download_coco8.py
    uv run python scripts/download_coco8.py --output data_files/coco8
"""

import argparse
import contextlib
from pathlib import Path
import sys


def download_coco8(output_dir: str) -> str:
    """
    Download COCO8 dataset using Ultralytics built-in downloader.

    Args:
        output_dir: Target directory to store the dataset.

    Returns:
        Path to the generated data.yaml file.
    """
    from ultralytics import YOLO
    from ultralytics.utils import DATASETS_DIR

    output_path = Path(output_dir).resolve()
    output_path.mkdir(parents=True, exist_ok=True)

    # Ultralytics downloads COCO8 to its default datasets directory
    # on first use.  We trigger the download by running a tiny
    # validation pass, then copy/symlink the result.
    print("Triggering COCO8 dataset download via Ultralytics...")

    # The simplest way: just load a tiny model and val on coco8.yaml.
    # Ultralytics auto-downloads the dataset on first reference.
    model = YOLO("yolov8n.pt")

    # Run a single-image validation to trigger the download
    with contextlib.suppress(Exception):
        model.val(data="coco8.yaml", imgsz=64, batch=1, verbose=False, plots=False, workers=0)

    # Find where Ultralytics placed the dataset
    coco8_dir = DATASETS_DIR / "coco8"
    if not coco8_dir.exists():
        # Try alternative names
        for candidate in ["coco8", "coco128"]:
            alt = DATASETS_DIR / candidate
            if alt.exists():
                coco8_dir = alt
                break

    if not coco8_dir.exists():
        print(f"Error: Could not find COCO8 dataset at {DATASETS_DIR}")
        print(f"Contents of {DATASETS_DIR}:")
        if DATASETS_DIR.exists():
            for item in DATASETS_DIR.iterdir():
                print(f"  {item.name}")
        sys.exit(1)

    print(f"COCO8 dataset located at: {coco8_dir}")

    # Create a data.yaml pointing to the Ultralytics download location
    data_yaml_content = f"""# COCO8 Mini Dataset for YOLO Benchmark Testing
# Auto-generated by download_coco8.py
# This is a tiny 8-image dataset for pipeline verification only.

path: "{coco8_dir.as_posix()}"

train: images/train
val: images/val

nc: 80
names:
  0: person
  1: bicycle
  2: car
  3: motorcycle
  4: airplane
  5: bus
  6: train
  7: truck
  8: boat
  9: traffic light
  10: fire hydrant
  11: stop sign
  12: parking meter
  13: bench
  14: bird
  15: cat
  16: dog
  17: horse
  18: sheep
  19: cow
  20: elephant
  21: bear
  22: zebra
  23: giraffe
  24: backpack
  25: umbrella
  26: handbag
  27: tie
  28: suitcase
  29: frisbee
  30: skis
  31: snowboard
  32: sports ball
  33: kite
  34: baseball bat
  35: baseball glove
  36: skateboard
  37: surfboard
  38: tennis racket
  39: bottle
  40: wine glass
  41: cup
  42: fork
  43: knife
  44: spoon
  45: bowl
  46: banana
  47: apple
  48: sandwich
  49: orange
  50: broccoli
  51: carrot
  52: hot dog
  53: pizza
  54: donut
  55: cake
  56: chair
  57: couch
  58: potted plant
  59: bed
  60: dining table
  61: toilet
  62: tv
  63: laptop
  64: mouse
  65: remote
  66: keyboard
  67: cell phone
  68: microwave
  69: oven
  70: toaster
  71: sink
  72: refrigerator
  73: book
  74: clock
  75: vase
  76: scissors
  77: teddy bear
  78: hair drier
  79: toothbrush
"""

    data_yaml_path = output_path / "coco8_data.yaml"
    with open(data_yaml_path, "w", encoding="utf-8") as f:
        f.write(data_yaml_content)

    print(f"Created data config at: {data_yaml_path}")
    print("\nTo use in autobenchmark, set data_yaml_path in your image data config to:")
    print(f"  {data_yaml_path.as_posix()}")

    return str(data_yaml_path)


def main():
    parser = argparse.ArgumentParser(
        description="Download COCO8 mini dataset for YOLO benchmark testing"
    )
    parser.add_argument(
        "--output",
        type=str,
        default="data_files/coco8",
        help="Directory to store the COCO8 dataset config (default: data_files/coco8)",
    )
    args = parser.parse_args()

    download_coco8(args.output)
    print("\nCOCO8 download complete!")


if __name__ == "__main__":
    main()
