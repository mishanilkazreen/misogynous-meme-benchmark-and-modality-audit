data_config_name: "text_data_tfidf"   # references config/data/text_data_tfidf.yaml

models_to_run: "all"

cv_settings:
  run_cv: true
  folds: 5

optimize_metric: "f1"
hp_optimization: "none"
save_models: true
